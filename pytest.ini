[pytest]
# Prevent pytest from recursing into the N.E.K.O repository (external build info)
norecursedirs = .git .venv N.E.K.O workspace

# Keep test discovery focused on the project's `tests/` directory
testpaths = tests
