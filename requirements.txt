# Roamin Ambient Agent — Python dependencies
# Install order matters for llama-cpp-python (see NOTE below)

# --- Core inference ---
# NOTE: llama-cpp-python requires manual CUDA build on this machine.
# Run scripts/install_llama_cpp_cuda.ps1 after pip install -r requirements.txt
# llama-cpp-python  <- installed separately

# --- Memory ---
# NOTE: mempalace requires chromadb<0.7 but chromadb 0.6.x is broken on Python 3.14
# (pydantic v1 incompatibility). Using chromadb 1.5.5 — mempalace search degrades
# gracefully until the upstream mempalace chromadb constraint is updated.
chromadb>=1.5.5
mempalace

# --- Voice ---
pyttsx3>=2.90
sounddevice>=0.4.6
openai-whisper>=20231117
keyboard>=0.13.5

# --- Screen observation ---
Pillow>=10.0.0
pytesseract>=0.3.10

# --- Voice Activity Detection ---
silero-vad>=5.1.2

# --- Wake Word Detection ---
openwakeword>=0.6.0

# --- HTTP (model fallback) ---
requests>=2.31.0

# --- GPU / Vision ---
torch>=2.6.0
numpy>=1.26.0

# --- System monitoring ---
psutil>=5.9.0

# --- Windows ---
pywin32>=306
winotify>=1.1.0
pystray>=0.19.0          # System tray icon + right-click context menu
Pillow>=10.0.0           # Already required; pystray uses it for icon images

# --- LM Studio SDK ---
lmstudio>=1.5.0

# --- Dev / Test ---
pytest>=8.0.0
black>=24.0.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0
pre-commit>=3.6.0
sympy==1.10.1           # Pinned: 1.14.0 has RecursionError in flake8 (issue #26692)

# Control API (development)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Testing
pytest-asyncio>=0.21.0
